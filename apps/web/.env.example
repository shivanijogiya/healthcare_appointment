# In dev, Vite proxies /api to the API, so this can stay unset.
# For a production build (vite build / vite preview) point it at the API:
VITE_API_URL=http://localhost:3000
VITE_CLINIC_OFFSET_MINUTES=330
